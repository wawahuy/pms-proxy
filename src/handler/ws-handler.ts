import {MayBePromise} from "../types";

// export type PPCallbackWsHandler = (request: PPServerRequest, response: PPServerResponse) => MayBePromise<void>;

export abstract class PPWsHandler {
    // abstract handle(request: PPServerRequest, response: PPServerResponse): MayBePromise<void>;
}
